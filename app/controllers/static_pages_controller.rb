class StaticPagesController < ApplicationController

	# The static pages are static (!) so there is not much control needed (in fact - none!)
	# Hence the controllers are just empty methods.
  def home
  end

  def help
  end

  def about
  end

  def contact
  end
  
end
