<!DOCTYPE html>
<html>
  <head>
    <title><%= full_title(yield(:title)) %></title>
    <%= stylesheet_link_tag "application", media: "all",
                                           "data-turbolinks-track" => true %>
    <%= javascript_include_tag "application", "data-turbolinks-track" => true %>
    <%= csrf_meta_tags %>
    <%= render 'layouts/shim' %>
    <!-- Stuff in other file - careful: this is Rails stuff (partials) and not native HTML! -->
    <!-- It causes the stuff in file _shim.erb to be included -->
  </head>
  <body>
    <%= render 'layouts/header' %>
    
    <div class="container">
    
      <!-- Include a div for each item set in the flash, to include appropriate messages -->
      <!-- Note that styling for the flash comes from Bootstrap - it is not in our css files -->
      <% flash.each do |key, value| %>
        <div class="alert alert-<%= key %>"><%=value %></div>
      <% end %>

      <!--This is where the page-specific content goes, achieved through the Ruby yield-->
      <%= yield %>
      
      <%= render 'layouts/footer' %>
    
      <!-- Debug box at the bottom of the page -->
      <%= debug(params) if Rails.env.development? %>
    </div>
  </body>
</html>