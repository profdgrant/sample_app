<!DOCTYPE html>
<html>
  <head>
    <title><%= full_title(yield(:title)) %></title>
    <%= stylesheet_link_tag "application", media: "all",
                                           "data-turbolinks-track" => true %>
    <%= javascript_include_tag "application", "data-turbolinks-track" => true %>
    <%= csrf_meta_tags %>
    <%= render 'layouts/shim' %>
    <!-- Stuff in other file - careful: this is Rails stuff (partials) and not native HTML! -->
    <!-- It causes the stuff in file _shim.erb to be included -->
  </head>
  <body>
    <%= render 'layouts/header' %>
    <div class="container">
      <!--This is where the page-specific content goes, achieved through the Ruby yield-->
      <%= yield %>
      <%= render 'layouts/footer' %>
      <!-- footer and header tags ARE keywords (built-in tags) in HTML5, and achieve their positioning by dint of the semantics of HTML5-->
    </div>
  </body>
</html>